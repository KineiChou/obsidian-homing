# Note Organizer 产品需求

状态：待实现的设计方案。用户需求包含 inbox 自动归档建议，以及编辑过程中通过 Jev 补齐内部链接的可行性设计。本文的默认值、范围和交互均为建议。

## 1. 目标与使用场景

用户把临时笔记、剪藏和零散记录放入 inbox，稍后需要判断每篇笔记应该归入已有的哪个目录。插件承担发现、比较和推荐的工作，用户保留最终归档决定。

核心流程：发现笔记 → 等待内容稳定 → Jev 推荐已有目录 → 用户检查或改选 → 确认移动 → 必要时撤销。

一篇笔记选择一个主要存放位置；横跨多个主题时，用户可以改选目录或保留待整理。归档功能不复制到多个目录，不改写正文或自动生成标签；内部链接由独立的推荐与确认流程插入。

编辑辅助的目标是让已经写下的概念指向库内合适笔记。自动发现可补的链接，用户确认后插入，保持原文措辞。归档与链接推荐分别启用，分别配置作用范围。

## 2. 四项原始需求的具体行为

| 原始需求 | 建议行为 | 需要明确的边界 |
| --- | --- | --- |
| 自动检查／设置 inbox | 设置页提供知识库内的目录选择器；首次运行可展示名称含 inbox 的候选，用户选定后开始监听 | 多个匹配时不猜测；路径不存在时显示重新选择或创建按钮；不自动修改 Obsidian 的新建笔记位置 |
| 获取 inbox 之外的文件夹 | 读取目录树，保留完整路径；目录新建、改名、移动、删除后更新 | 自动发现的目录还需要排除 inbox 子树、附件、模板及用户排除的子树；只比较目录名字会混淆同名目录 |
| Jev 根据笔记分类 | 将当前笔记和可归档目录的用途提供给 Jev，由它选择既有候选 | 必须有“暂无合适目录”；结果可能不符合用户意图；首版不自动创建目录 |
| 提供用户确认 | 在待归档列表中展示原路径、目标路径、预览和其他候选，支持确认、改选、稍后、忽略 | 只有点击确认才移动；关闭界面不执行；确认时重新检查内容和路径 |

建议首版只支持一个 inbox，默认包含其子目录，设置中可以关闭子目录监听。`Inbox-old` 不属于 `Inbox`；根目录不能作为 inbox，以免把整个知识库都当作待整理内容。

## 3. 用户流程与确认体验

### 首次设置

1. 选择 inbox，并展示本次监听范围。
2. 展示自动发现的可归档目录，允许排除指定子树；附件和模板目录的排除范围可检查、修改。
3. 为容易混淆的目录补充一句用途说明，可选填“适合什么／不适合什么”；仅用于组织子目录的容器可关闭直接归档。先允许仅使用完整路径运行。
4. 配置 TypeSafe 凭据，并用插件自带的示例文本测试连接。
5. 展示将发送的字段，用户开启自动分析后开始处理新进入的笔记。
6. inbox 已有笔记单独显示数量，由“分析已有笔记”启动，避免首次开启时产生大量请求。

### 触发规则

- 新建笔记、从库外导入、从其他目录移入 inbox 都属于新进入；不限于当前打开的笔记。
- 建议内容停止变化 10 秒后再分析；仍在活动编辑器中编辑的笔记继续等待，切离后再处理。用户可以手动执行“分析当前笔记”。10 秒是待试用调整的默认值。
- 内容指纹未改变的重复事件不重新请求；正文为空的笔记等待内容，不发送请求。模板占位文本不保证可被自动识别，可通过用途判断返回“暂无合适目录”。
- 建议产生后内容再有变化，旧建议立即失效；新内容稳定后重新分析。用户手动请求可以重试相同内容。
- 重启后恢复待办状态；未记录的历史笔记显示在已有笔记入口，不自动批量分析。
- 暂停自动分析后不再启动请求；已有建议仍可查看、人工改选和确认。恢复后明确显示积压数量。

### 待归档列表

默认在侧栏集中处理，并用状态栏显示待处理数量，避免每来一篇笔记就弹窗。

单篇确认卡片示例：

```text
Transformer 学习笔记

当前位置：Inbox/Transformer 学习笔记.md
推荐位置：Resources/机器学习/Transformer 学习笔记.md
目录用途：模型原理、论文和通用学习资料

其他候选：Projects/课程作业 · Resources/计算机科学

[查看笔记] [更改目录] [确认归档] [稍后] [忽略此笔记]
```

“目录用途”来自用户填写的目录说明。Jev 不生成自由文本，因此首版不展示杜撰的分类理由。界面可展示本地笔记预览和候选目录；模型评分放在可展开的详情中。[Jev 介绍](https://docs.typesafe.ai/introduction)

- 确认归档：将当前确认卡片所显示的笔记移动到所显示的最终路径。
- 更改目录：从全部有效可归档目录中人工选择；可以不再调用 Jev。
- 稍后：保留在列表中，本次不再提醒。
- 忽略此笔记：停止该笔记的自动分析；重启和内容更新后仍有效，可在设置中恢复。笔记在当前会话中改名时跟踪；跨设备或离线改名的身份恢复属于后续范围。
- 无合适目录：保留原位，提供人工选择和稍后处理。
- 关闭面板或按 Esc：不移动。
- 键盘可完成选择、确认和跳过；不占用用户已有快捷键。

首版逐篇确认；后续可增加多选确认。批量移动也需要展示每篇的最终目标，报告各自结果，不能把部分成功当成全部成功。

## 4. 分类设计：目录结构不等于分类规则

### 目录语义

只提供“AI”“学习”“工作”等名字，无法判断用户希望按主题、项目还是使用目的归档。例如同一篇机器学习笔记可能属于“通用资料”，也可能属于“当前课程作业”。若这个意图没有写在笔记或目录说明里，模型无法可靠推断。

建议向 Jev 提供：笔记标题、正文、标签等明确选定的属性，以及每个候选的完整路径和可选用途说明。目录选择优先匹配笔记的主要用途，再匹配主题；没有证据时返回不匹配或交给用户改选。父目录的分支范围与直接存放用途需要区分；深度本身不代表合适程度，仅用于组织子目录的容器不作为最终目标。

首版不读取其他笔记正文来推断目录含义。后续只有在路径加说明仍明显不足时，再考虑用户挑选的示例笔记。人工改选可以用于本地评估，但不会自动训练 Jev 或擅自改写分类规则。

### Jev 的决策范围

Jev 的 Choice 适合从已有目录集合中选一个选项，并返回各候选的概率分布。单个 Choice 上限为 255，因此预留“暂无合适目录”后，一题最多包含 254 个可归档目录；这不是知识库的目录总量上限。[Choice 官方文档](https://docs.typesafe.ai/primitives/choice)

- 推荐混合深度分类：所有允许直接存放笔记的目录都是候选，完整路径提供层级上下文，不先用顶层名字排除整条分支。
- 全部候选不超过 254 且上下文预算允许时，一次 Choice 比较全部候选。
- 超出数量或单题上下文预算时，把实际目标目录分组，各组都参与提名，再将每组多个候选放入同一个 Choice 决选。初始参数为每组最多 64 个目录、每组保留前三，参数需验证；不能直接比较不同组的概率。
- 分组提名仍可能遗漏正确目录。首版使用有界的两阶段流程；超过组数、候选或请求预算时提示缩小范围或人工归档，不静默截断，不继续叠加淘汰轮次。
- 单路径逐层判断不作为默认方案；保留多个分支的层级搜索只在大规模且语义层级清晰的目录树中另行评估。完整算法、约束与验证方法见 [目录分类设计](docs/folder-classification.md)。
- 首版只读取 Markdown 文本；图片、PDF、音频和 Canvas 不进入自动分类流程。

`confidence` 表示候选概率分布的集中程度，不等于“这次归档有多少正确率”。首版不依赖未经验证的阈值自动移动；所有建议仍经过用户确认。[Confidence 官方说明](https://docs.typesafe.ai/confidence)

官方说明目前英语表现更好，中文、日文等内容需要实际验证。因此中文及中英混合笔记必须进入首轮样本。[模型说明](https://docs.typesafe.ai/models)

## 5. 容易遗漏的边界

| 情况 | 预期行为 |
| --- | --- |
| inbox 改名／删除 | 能明确跟踪的改名同步更新设置；删除或无法识别时暂停监听并显示路径异常，不转为全库扫描 |
| 同名子目录／父子目录都可存笔记 | 使用完整路径；保留允许存笔记的父目录，不强制归到叶子节点 |
| 文件夹整体移入 inbox | 重新检查受影响子树中的 Markdown 笔记，加入待处理范围 |
| 候选目录为空 | 不请求模型，显示需要选择可归档目录 |
| 目标目录已有同名文件 | 不覆盖、不合并；让用户改名、改选目录或取消，显示最终路径后才能确认 |
| 请求期间笔记被修改、移动或删除 | 丢弃旧结果，不凭过期路径移动文件 |
| 等待确认期间目录树／用途说明改变 | 旧建议失效，更新后重新分析；人工选择仍需校验最新路径 |
| AI 没有合适建议／笔记横跨多个主题 | 保留 inbox，允许人工选一个主要位置；不强制分类、不复制文件 |
| 网络断开、额度不足、凭据错误、限流 | 保留笔记，区分错误并提供重试；凭据／额度问题不循环重试 |
| 相同事件重复触发／用户连续点击确认 | 同一笔记只保留一个有效处理任务，一次确认最多移动一次 |
| 插件自身移动事件被再次监听 | 已移出 inbox 的笔记退出处理队列；撤销回 inbox 后保持暂停，避免自动重复归档 |
| 应用重启／移动中断 | 恢复待处理记录；结合源路径和目标路径核对移动记录，不盲目重放文件操作 |
| 多设备同步 | 首版建议只在一个指定设备启用自动分析；其他设备不执行队列任务，避免重复调用与冲突 |
| 超长笔记 | 超出本地预算时标记需人工处理；首版不静默截断正文、也不增加另一个模型摘要 |

### 移动、链接、附件与撤销

移动应经过 Obsidian 的 `FileManager.renameFile`，链接更新遵循用户的 Obsidian 设置。不能承诺任意外部程序或插件引用都能自动修复。[官方 API](https://github.com/obsidianmd/obsidian-api/blob/master/obsidian.d.ts)

首版只移动笔记，附件保留原位；必须验证常见内部链接、相对 Markdown 链接、图片引用、标题和块引用的行为。当前设置无法保证引用继续有效时，明确提示并阻止静默移动。

记录归档前后的路径与执行结果，提供“撤销本次归档”。撤销只反向移动文件，不恢复旧正文，避免覆盖后续编辑。原位置已被占用、目标文件被删除或另行移动时，停止撤销并说明冲突。

### 数据与费用

首次开启自动分析前，展示发送到 TypeSafe 的字段：当前笔记标题、正文、选定属性、候选目录路径及说明。目录名称本身也可能含有私人信息。

提供笔记排除和暂停功能；凭据不写入知识库笔记、普通日志或提交到 Git。分类记录只保留必要的路径、状态、模型信息和指纹，不复制整篇笔记。

TypeSafe 声明不使用输入训练模型，但其一般隐私政策允许为服务等目的保留数据，不能把“不训练”描述为“零保留”。[隐私政策](https://typesafe.ai/legal/privacy-policy)

首版串行分析，每篇每个有效内容版本最多一个自动分类任务；任务可以包含分组提名和决选的多个 API 请求。执行前检查任务的请求与 token 预算，显示实际请求数和已返回的输入 token 用量。费用估算与实际账单分开标示，失败重试也可能产生费用。

## 6. 编辑时的双链补齐

可行性判断：明确提及已有笔记标题或别名时，可以由本地索引召回目标，再通过 Jev 结合上下文判断是否链接及最终目标。对于只存在隐含语义联系的内容，需要更强的检索和文本片段识别，首版不承诺完整覆盖。

这里的双链是插入当前笔记到目标笔记的内部链接，Obsidian 负责展示反向链接；无需同时改写目标笔记。[Backlinks 官方说明](https://help.obsidian.md/plugins/backlinks)

建议流程：编辑停顿 → 读取当前段落 → 识别可链接词句 → 本地召回少量笔记 → Jev 选择一个目标或放弃 → 展示预览 → 用户确认插入。

- 首版使用名称与 `aliases` 的压缩前缀树识别文字位置，词条直接关联候选文件；使用局部上下文与候选元数据消歧。自动查询不扫描全库、不读候选正文、不持久化每次查询。
- 每个文本位置给 Jev 约 5–20 个候选，加一个“无需链接”；整个知识库可以远大于 255 篇笔记。
- 推荐只针对确有文字位置且语义相符的内部链接；泛泛相关的笔记放入后续的“相关笔记”能力，不强行包装成双链。
- 排除已有链接、代码、公式、属性等区域，默认同一目标在当前笔记中不重复推荐。
- 编辑改变、切换笔记、目标移动或删除后旧建议失效；插入必须经过编辑器事务，可用正常撤销恢复。
- 自动分析包含尚未保存的局部文字与候选元数据；候选正文只在用户手动选择补充内容时限量读取，须展示新增的发送范围。
- 链接候选范围与归档候选范围独立。例如，归档时排除的历史资料仍可能是有用的链接目标。

完整流程见 [双链补齐可行性](docs/link-suggestions.md)；四种算法的比较、增量索引、资源预算和合成实验见 [轻量索引设计](docs/link-indexing.md)。

## 7. 首版范围与后续范围

**首版建议：** 桌面端、单个 inbox、Markdown、自动目录发现与排除、可选目录说明与容器设置、Jev 混合深度分类、后台待办队列、逐篇确认／人工改选、错误重试、路径冲突处理、移动记录与撤销。预算内全量直接比较，超出单题限制时使用有界的分组提名与统一决选。

**随后的小版本：** 编辑时针对明确标题／别名提及的双链建议，支持 Jev 消歧、确认插入、忽略和编辑器撤销。可以与归档共享 Jev 配置与请求预算，但保持独立开关和任务状态。

**后续按需要增加：** 多选确认、多 inbox、面向更大目录树的多分支搜索、用户选定的目录示例、语义检索补链、相关笔记推荐、移动端与多设备协调。

**单独评估的能力：** 高置信度自动移动、创建新目录、重命名笔记、附件搬运、历史知识库重组、自动学习规则。这些都会改变用户对插件权限和行为的理解，不应混入首版。

插件工作名称为 Note Organizer，独立于现有 Vim 插件。技术设计选用 TypeScript、esbuild、Obsidian 原生 UI 与 CodeMirror 6；设计最低 Obsidian 版本为 1.11.4，凭据通过 SecretStorage 管理。发布前确定名称、许可证并验证最低版本兼容性。模块接口与实现边界见 [技术设计](docs/technical-design.md)。

## 8. 验收与假设验证

### 功能验收

1. 配置 inbox 后，创建、导入、移入的 Markdown 笔记能进入队列；其他目录和非 Markdown 文件不触发分类。
2. inbox 子树与排除子树不会成为目标；同名目录按完整路径区分。
3. 用户看到并确认最终路径后才移动；取消、忽略、异常、旧结果都不移动文件。
4. 同名冲突不会覆盖文件；重复点击和重复事件不会重复移动。
5. 推荐产生后修改笔记或目录配置，旧推荐不能继续执行。
6. 常见链接和图片引用通过真实 Obsidian 验证；撤销保留最新正文，冲突时不覆盖。
7. 重启后可恢复待办并核对中断操作；不误把全部已有笔记当成新笔记批量请求。
8. 超长内容、候选超限、空候选、连接失败均有明确状态与人工处理入口。
9. 分组分类覆盖当前范围内所有有效目录；决选只使用本次提名结果，任何组失败都不能被当成“该组无匹配”。内容或目录版本在任一阶段变化后，整个旧任务失效。

### 需要用真实笔记验证的假设

| 假设 | 当前把握 | 最小验证方法 |
| --- | --- | --- |
| 现有目录表达了清晰且一致的归档规则 | 未验证 | 先由用户给 30–50 篇笔记标记可接受目录，也允许“无法判断” |
| 完整路径足够分类，用途说明只在少数目录必要 | 未验证 | 对同一批样本比较“路径”与“路径＋人工用途说明”；收益小则保留简单方案 |
| Jev 能处理中文、技术术语和中英混合笔记 | 未验证 | 按语言和笔记类型分别记录首选命中率、前三候选命中率及“无合适目录”表现 |
| 候选评分能反映本知识库的歧义 | 未验证 | 比较评分分布与实际改选，尤其观察高评分的错误案例；不预设准确率等于 confidence |
| 分组提名能保留最终需要的目标目录 | 未验证 | 比较候选召回率、最终首选命中率和分组变化后的稳定性；分别记录提名遗漏与决选错误 |
| 确认步骤节省了归档时间 | 未验证 | 比较人工归档与推荐后确认的时间，同时记录改选、跳过和撤销次数 |

样本评估须使用用户允许发送的内容；先独立标注可接受目录，再看模型结果。结果用于确定候选范围和交互，不在几十个样本上宣称普遍准确率。功能底线是未确认移动、覆盖内容和静默丢失任务均为零。

## 9. 实现前待确定的信息

- 实际可归档目录数量、用途说明长度，以及是否混用项目、领域、资源等分类方式。这决定直接比较或分组比较的预算，以及是否需要为容器与具体目标分别补充说明。
- inbox 是否包含子目录，是否有多个收集入口。
- 用户希望排除的目录、属性及私密笔记范围。
- 双链建议是否对 inbox 之外正在编辑的笔记启用；建议可选全库，但遵循独立排除范围。
- 现有笔记是否维护 aliases，以及是否允许发送少量候选笔记的相关摘录。
- Obsidian 版本、使用设备和 TypeSafe 账户访问能力。连接测试先使用示例文本，凭据由用户在插件设置中填写。

这些信息影响默认值或首版范围；需求设计本身不需要读取真实笔记或调用付费分类接口。
